function Restart({stopGame}) {
    return (
        <div className="restart">
             <button onClick={stopGame}>Restart game</button>
        </div>
           
      
    )
}

export default Restart;